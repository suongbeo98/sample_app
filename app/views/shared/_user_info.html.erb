<%= link_to gravatar_for(current_user, size: Settings.gravatar.users_index_size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".link_profile"), current_user %></span>
<span><%= pluralize current_user.microposts.size, t(".count_microposts") %></span>
